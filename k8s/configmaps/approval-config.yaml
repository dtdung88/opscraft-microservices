apiVersion: v1
kind: ConfigMap
metadata:
  name: approval-config
  namespace: opscraft
data:
  SERVICE_NAME: "approval-service"
  SERVICE_PORT: "8008"
  LOG_LEVEL: "INFO"
  LOG_FORMAT: "json"
  DEFAULT_APPROVAL_EXPIRY_HOURS: "72"
